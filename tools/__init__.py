"""Diff test framework — TRT vs HuggingFace comparison tools.

Shared helpers:
  tool_helpers      — engine build, HF model loading, array comparison
  diffusion_helpers — activations, bundle parsing, TRT engine execution
"""
