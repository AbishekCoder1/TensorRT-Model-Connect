# Bundle Helpers

`bundle_helpers.*` provides shared bundle and engine setup utilities.

Key responsibilities:
- Bundle section lookup (`find_bundle_sections`)
- Tokenizer extraction/loading
- Common TensorRT decoder-engine construction helpers
